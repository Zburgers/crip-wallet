# Local fake-money development only. dev:up generates .local/runtime.env; that
# ignored file, not this example, is the authoritative effective configuration.
CRIP_ENVIRONMENT=local
CRIP_CHAIN_ID=eip155:31337
CRIP_POSTGRES_HOST=127.0.0.1
CRIP_POSTGRES_DATABASE=crip_wallet
CRIP_POSTGRES_USER=crip
CRIP_POSTGRES_PASSWORD=
